Django==5.1.5
django-cors-headers>=4.3.1
django-environ==0.11.2
redis==5.0.1
django-redis>=5.4.0
celery==5.3.4
django-celery-beat==2.7.0
django-celery-results==2.5.1
locust==2.17.0
psycopg2-binary>=2.9.9
Pillow==10.3.0
whitenoise==6.7.0
djangorestframework>=3.15.2
djangorestframework-simplejwt>=5.3.1
django-filter>=24.3
django-extensions>=3.2.3
stripe>=7.0.0
cashfree-pg>=4.0.0
paytmchecksum==1.7.0
requests>=2.31.0

# Production observability & documentation
sentry-sdk[django]>=1.40.0
drf-spectacular>=0.27.0
prometheus-client>=0.20.0

# OpenTelemetry — optional, uncomment to enable distributed tracing
# opentelemetry-sdk>=1.24.0
# opentelemetry-exporter-otlp-proto-grpc>=1.24.0
# opentelemetry-instrumentation-django>=0.45b0
# opentelemetry-instrumentation-redis>=0.45b0
# opentelemetry-instrumentation-celery>=0.45b0
# opentelemetry-instrumentation-requests>=0.45b0

# ── Search Engine (System 9) ─────────────────────────────────────────────────
elasticsearch>=8.12.0,<9.0.0

# ── Push Notifications (System 8) ────────────────────────────────────────────
# FCM via HTTP — no extra library needed (uses requests)
# firebase-admin>=6.0.0  # Uncomment for service account auth

# ── PDF Invoice Generation (System 14) ───────────────────────────────────────
# weasyprint>=60.0  # Uncomment for HTML→PDF (requires libpango, libcairo)
# reportlab>=4.0.0  # Uncomment for programmatic PDF generation

# ── SMS / Notifications (System 8) ────────────────────────────────────────────
# twilio>=8.0.0     # Uncomment for Twilio SMS
# msg91-python>=1.0.0  # Uncomment for MSG91 SMS (India)

# ── QR Code (Vouchers) ────────────────────────────────────────────────────────
qrcode[pil]>=7.4.2

# ── Date utilities ────────────────────────────────────────────────────────────
python-dateutil>=2.8.2

# ── Cryptography (webhook signature verification) ──────────────────────────────
cryptography>=41.0.0
gunicorn==21.2.0
